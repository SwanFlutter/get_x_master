name: get_x_master
description: "GetX for Flutter enhances state management, routing, and dependency injection with improved performance and compatibility with the latest updates."
version: 0.0.8
homepage: "https://github.com/SwanFlutter/get_x_master"

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ^1.1.0
  intl: ^0.20.2
  shamsi_date: ^1.0.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:


